This directory is for audio assets.

Required files (to be provided by project owner):
- bgm.ogg  — BGM loop, 15-30s seamless, OGG Vorbis format
- bgm.m4a  — BGM loop, same audio, M4A/AAC format (Safari compatibility)

Current sound files are in /sound/ directory (WAV format, to be converted).
